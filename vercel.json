{
  "buildCommand": "prisma migrate resolve --rolled-back 20250820000000_fix_userorg_enum_columns || true && prisma generate && prisma migrate deploy && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/reset-usage",
      "schedule": "0 0 1 * *"
    }
  ]
}
